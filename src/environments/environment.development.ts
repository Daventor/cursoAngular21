export const environment = {
    production: false,
    appSettings: {
        apiUrl: 'https://jsonplaceholder.typicode.com',
        timeout: 5000,
        featureFlags: {
            darkMode: false,
            enableChat: true
        }
    }
};
